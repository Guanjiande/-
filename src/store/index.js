import Vuex from 'vuex'
import Vue from 'vue'

Vue.use(Vuex)

const state = { // 全局变量
}

const getters = {
}

const actions = {

}

const mutations = {

}
export default new Vuex.Store({
  state,
  getters,
  actions,
  mutations
})
